import { useEffect } from "react";
import data from "../data";
const updateFullStateReducer = () => {
  useEffect(() => {
    setTimeout(() => s);
  }, []);
};

export default updateFullStateReducer;
